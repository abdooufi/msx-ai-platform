node_modules/
frontend/node_modules/
.next/
dist/
build/
.env

# Persistent data – bind-mounted into Docker containers.
# NEVER commit these; they contain databases, vector indexes, and uploaded files.
data/